# CONFIGURACIÓN PARA EL AMBIENTE DE PRODUCCIÓN (RENDER)

spring.application.name=demo

# --- 1. CONEXIÓN A POSTGRESQL ---

# URL de conexión (SIN credenciales en el string)
spring.datasource.url=jdbc:postgresql://dpg-d527g0u3jp1c73btp7ig-a.virginia-postgres.render.com/exfullstack2


# Driver de PostgreSQL
spring.datasource.driverClassName=org.postgresql.Driver

# Credenciales de Render
spring.datasource.username=exfullstack2_user
spring.datasource.password=4kvM5fy3uZ4E86CVkMbAYoef0SpxttUm

# --- 2. CONFIGURACIÓN DE JPA / HIBERNATE ---

# Especifica el dialecto de PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Control de la creación/actualización del esquema en producción
spring.jpa.hibernate.ddl-auto=update

# --- 3. CONFIGURACIÓN DE SERVIDOR ---
# Hereda el valor de la variable PORT, si no existe, usa 8080 (solo si la app falló en cargar el perfil base)
server.port=${PORT:8080}

# Open-In-View también se desactiva en producción
spring.jpa.open-in-view=false

